import { Building2, ChevronLeft, Home, Palette, Piano, UserRound } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { startDemoScenario, startStudentDemo } from '../data/demoSession';
import { DEMO_SCENARIOS } from '../data/demoScenarios';

const cards=[{id:'study-room' as const,icon:Home,summary:'학생 12명 · 상담 중심 운영'},{id:'art' as const,icon:Palette,summary:'수강생 16명 · 작품 피드백 중심'},{id:'piano' as const,icon:Piano,summary:'수강생 18명 · 연습 관리 중심'}];
export default function DemoPage(){const navigate=useNavigate();const startOperator=(id:typeof cards[number]['id'])=>{startDemoScenario(id);navigate('/',{replace:true});};const student=()=>{startStudentDemo();navigate('/',{replace:true});};return <div className="page-container bg-[#F8F5F0]"><main className="min-h-[100dvh] px-5 pb-8 pt-[max(2rem,env(safe-area-inset-top))]"><h1 className="text-2xl font-bold text-[#14233B]">데모로 둘러보기</h1><p className="mt-2 text-sm text-[#687282]">실제 화면을 체험해 보세요.</p><section className="mt-8"><h2 className="text-sm font-bold text-[#14233B]">운영자 데모</h2><div className="mt-3 space-y-2">{cards.map(card=>{const scenario=DEMO_SCENARIOS.find(s=>s.id===card.id)!;const Icon=card.icon;return <button key={card.id} onClick={()=>startOperator(card.id)} className="flex min-h-20 w-full items-center gap-4 rounded-[13px] border border-[#E1DBD3] bg-[#FFFDFC] p-4 text-left"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-[#F1E7D6] text-[#A67B3F]"><Icon size={20}/></span><span><strong className="block text-sm text-[#14233B]">{scenario.name}</strong><span className="mt-1 block text-xs text-[#687282]">{card.summary}</span></span></button>;})}</div></section><section className="mt-8"><h2 className="text-sm font-bold text-[#14233B]">학생 데모</h2><button onClick={student} className="mt-3 flex min-h-28 w-full items-center gap-4 rounded-[13px] border border-[#C89B55] bg-[#FFFCF7] p-4 text-left"><span className="flex h-14 w-14 items-center justify-center rounded-full bg-[#F1E7D6] text-[#A67B3F]"><UserRound size={25}/></span><span className="min-w-0 flex-1"><strong className="block text-sm text-[#14233B]">미술 학원 학생</strong><span className="mt-1 block text-xs text-[#687282]">한유진 학생 · 수채화반</span><span className="mt-2 block text-[11px] font-semibold text-[#A67B3F]">완료 3건 · 진행 중 3건 · 수정 요청 1건</span></span></button></section><button onClick={()=>navigate('/start')} className="mt-8 flex min-h-12 w-full items-center justify-center gap-1 text-xs font-bold text-[#53606F]"><ChevronLeft size={15}/>이전으로 돌아가기</button></main></div>;}
